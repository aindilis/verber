;; Planning out for todays tasks, so that I can figure out what I need
;; to do and when, given the constraints

;; (include LAUNDRY)

(define (domain INTEGRATE)
  (:requirements)
  (:types)
  (:predicates)
  (:functions)	
  )
