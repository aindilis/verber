;; <domain-file>

(define (domain )

  (:requirements)

  (:types)

  (:predicates
   (finished g - goal)
  )

  (:predicates)

  (:functions)

  (:derived)

  (:durative-action)

  )

;; </domain-file>
