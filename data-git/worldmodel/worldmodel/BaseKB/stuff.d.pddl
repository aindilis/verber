;; <domain-file>

(define (domain )

  (:requirements)

  (:types
   shoes socks pants shirt coat - clothing
   laptop headset power-supply power-cord - )

  (:predicates
   (finished g - goal)
  )

  (:predicates)

  (:functions)

  (:derived)

  (:durative-action)

  )

;; </domain-file>
